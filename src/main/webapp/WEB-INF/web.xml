<?xml version="1.0" encoding="utf-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://www.w3.org/2001/XMLSchema-instance"
         version="3.1">

    <display-name>My First Web Application</display-name>

    <welcome-file-list>
        <welcome-file>index.html</welcome-file>
    </welcome-file-list>

    <!-- Error pages -->
    <error-page>
        <error-code>500</error-code>
        <location>/error/500.html</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/error/404.html</location>
    </error-page>

    <!-- Jersey REST configuration -->
    <servlet>
        <servlet-name>MainServices</servlet-name>
        <servlet-class>org.glassfish.jersey.servlet.ServletContainer</servlet-class>

        <!-- CORS and other filters -->
        <init-param>
            <param-name>jersey.config.server.provider.classnames</param-name>
            <param-value>pt.unl.fct.di.apdc.firstwebapp.filters.AdditionalResponseHeadersFilter</param-value>
        </init-param>

        <!-- Pacote com os recursos REST -->
        <init-param>
            <param-name>jersey.config.server.provider.packages</param-name>
            <param-value>pt.unl.fct.di.apdc.firstwebapp.resources</param-value>
        </init-param>

        <!-- Ativa o tracing (útil para debug) -->
        <init-param>
            <param-name>jersey.config.server.tracing.type</param-name>
            <param-value>ALL</param-value>
        </init-param>

        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- Mapeamento da servlet Jersey -->
    <servlet-mapping>
        <servlet-name>MainServices</servlet-name>
        <url-pattern>/rest/*</url-pattern>
    </servlet-mapping>

    <!-- Exemplo de área protegida (opcional) -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Restricted Area</web-resource-name>
            <url-pattern>/secret/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>admin</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- Configuração do AppStartupListener -->
    <listener>
        <listener-class>pt.unl.fct.di.apdc.firstwebapp.listeners.AppStartupListener</listener-class>
    </listener>

</web-app>
